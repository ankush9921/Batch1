#include<stdio.h>
int main()
{
              // take a positive integer input and tell if it even or odd
    int n;
    printf("enter a number:");
    scanf("%d",&n);
    if(n%2==0)
    {
    	printf("enter the even number:");
	}
	else
	{
		printf("enter the odd number:");
	}
	return 0;
}
