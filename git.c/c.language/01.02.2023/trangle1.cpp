#include<stdio.h>

int main()
{
printf("Enter the Base Length of Triangle: ");
b = float(input());
printf("Enter the Height Length Triangle: ");
h = float(input());
a = 0.5 * b * h;
print("\nArea = ", a);

return 0;
}
